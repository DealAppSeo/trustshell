{
  "name": "@hyperdag/trustshell",
  "version": "0.4.0",
  "description": "Trust scoring SDK for AI — add HAL evaluation to any LLM app in 3 lines. Includes TypeScript + Python clients, CLI, and framework integrations.",
  "private": false,
  "main": "src/lib/index.ts",
  "types": "src/lib/index.ts",
  "scripts": {
    "dev": "next dev",
    "build": "next build && echo 'Next app built. For SDK: tsc -p tsconfig.sdk.json (if configured)'",
    "start": "next start",
    "lint": "eslint",
    "sdk:build": "tsc --project tsconfig.sdk.json",
    "sdk:test": "jest --testPathPattern=trustshell",
    "prepublishOnly": "npm run sdk:build && npm run sdk:test"
  },
  "dependencies": {
    "@hyperdag/proof-verifier": "0.2.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.5",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
