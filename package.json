{
  "name": "@hyperdag/trustshell",
  "version": "0.5.0",
  "description": "Trust SDK for AI agents — guest-first onboarding (no wallet/email/signup), HAL evaluation on free models, ZKP-verifiable RepID, deferred claim. CLI + TypeScript client.",
  "private": false,
  "main": "src/lib/index.ts",
  "types": "src/lib/index.ts",
  "bin": {
    "trustshell": "dist/cli.js"
  },
  "files": [
    "dist",
    "src/lib",
    "README.md"
  ],
  "scripts": {
    "dev": "next dev",
    "build": "next build && echo 'Next app built. For SDK: tsc -p tsconfig.sdk.json (if configured)'",
    "start": "next start",
    "lint": "eslint",
    "sdk:build": "tsc --project tsconfig.sdk.json",
    "sdk:test": "jest --testPathPattern=trustshell",
    "prepublishOnly": "npm run sdk:build && npm run sdk:test"
  },
  "dependencies": {
    "@hyperdag/proof-verifier": "0.2.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/jest": "^29",
    "@types/node": "^20",
    "jest": "^29",
    "ts-jest": "^29",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.5",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
