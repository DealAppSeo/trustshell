[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hyperdag-trustshell"
version = "0.3.0"
description = "TrustShell Python SDK"
requires-python = ">=3.8"
dependencies = ["httpx>=0.24.0"]
