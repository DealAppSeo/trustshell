# hyperdag-trustshell (Python)

```python
from trustshell import TrustShell
shell = TrustShell()
result = shell.score("The capital of France is Paris.")
print(result.trust_score)  # 87
```

See parent README_SDK.md for full docs.
Built in S-BUILD.
