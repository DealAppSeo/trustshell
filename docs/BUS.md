# BUS — XC1 overnight 2026-09-25

GA is down. Pick tickets in order. Off current main. Shadow only.

## Hard rules
- No HeyGen. No Railway. No live stake.
- REAL_STAKING_ENABLED stays off.
- Missing data = NOT_CHECKED, never 0.
- When 3 PRs are up, start the next 3. Never idle if a ticket remains.

## Tickets
1. Landing close copy: "Your agent. Your keys. A receipt when it's right or wrong." End card text THERE IS NO TRY. Do not say VETO in the headline.
2. After-create: next step is verify a claim, then look at the receipt. Wallet/stake labeled testnet / shadow. No "stake now."
3. Model card: Honesty A keeps TRUE / FALSE / NOT_CHECKED separate. First-pass vs post-HAL are different columns when the engine exposes them; else NOT_CHECKED. Help B stays no ratings.
4. docs/FIRST_PASS.md — stranger page. First-pass vote vs post-HAL verdict. Cite the engine route or mark NOT_CHECKED. Do not invent columns.
5. trustshell status prints that distinction or prints NOT_CHECKED.
6. Tests fail if THERE IS NO TRY is spoken as a product claim or if can_stake is shown live.
